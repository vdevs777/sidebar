import { PageHeader } from "@/components/page-header";
import { User } from "lucide-react";

export function CostumersMaps() {
    return (
        <div>
            <PageHeader path="Clientes /" title="Mapas" icon={<User />} />
        </div>
    )
}